import { AreaTop } from "../../components";

const Section = () => {
  const handleTitle = {
    title: "Section",
  };
  return (
    <>
      <AreaTop handleTitle={handleTitle} />
    </>
  );
};

export default Section;
